name: CI/CD on College Server

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: self-hosted   # target your server's runner
    steps:
      - uses: actions/checkout@v4

      - name: Run tests
        run: |
          echo "Run tests here"
          # npm test

      - name: Deploy app
        run: |
          cd /var/www/your-app
          git pull origin main
          # restart your service here
